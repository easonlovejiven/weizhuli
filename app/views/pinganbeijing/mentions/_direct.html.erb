  <% if @mention_uid.blank?%>
    <div class="row-fluid search-box radius-3">
      <div class='title'>
        <strong>筛选条件</strong>
      </div>
      <div class="span12 conditions">
        <%=form_for :search, :url=>pinganbeijing_mentions_path,:method=>:get do |form|
          %>
        <div class="tags">
          <!--
          <span>
            <div class="btn-group" data-toggle="buttons-checkbox" data-bind="is_friends" data-single-select>
                <button type="button" class="btn btn-mini" data-val="yes">是</button>
                <button type="button" class="btn btn-mini" data-val="no">否</button>
            </div>
            <lable>我的关注</lable>
            <%=form.hidden_field :is_friends, :value=>@q[:is_friends]%>
            <%#=form.select :is_friends,options_for_select([["是否我的关注",""],["是",'yes'],["否","no"]],@q[:is_friends])%>
            
          </span>
        -->
          <span>
            <div class="btn-group" data-toggle="buttons-checkbox" data-bind="is_verified" data-single-select>
                <button type="button" class="btn btn-mini" data-val="yes">是</button>
                <button type="button" class="btn btn-mini" data-val="no">否</button>
            </div>
            <lable>认证用户</lable>
            <%=form.hidden_field :is_verified, :value=>@q[:is_verified]%>
            <%#=form.select :is_verified,options_for_select([["是否认证用户",""],["是",'yes'],["否","no"]],@q[:is_verified])%></span>
          <!--
          <span>
            <div class="btn-group" data-toggle="buttons-checkbox" data-bind="has_interactions" data-single-select>
                <button type="button" class="btn btn-mini" data-val="yes">有</button>
                <button type="button" class="btn btn-mini" data-val="no">无</button>
            </div>
            <lable>互动</lable>
            <%=form.hidden_field :has_interactions, :value=>@q[:has_interactions]%>
            <%#=form.select :has_interactions,options_for_select([["是否互动过",""],["是",'yes'],["否","no"]],@q[:has_interactions])%></span>
          <span>
            <div class="btn-group" data-toggle="buttons-checkbox" data-bind="has_avatar" data-single-select>
                <button type="button" class="btn btn-mini" data-val="yes">有</button>
                <button type="button" class="btn btn-mini" data-val="no">无</button>
            </div>
            <lable>头像</lable>
            <%=form.hidden_field :has_avatar, :value=>@q[:has_avatar]%>
            <%#=form.select :has_avatar,options_for_select([["是否有头像",""],["是",'yes'],["否","no"]],@q[:has_avatar])%></span>
          <span>
            <div class="btn-group" data-toggle="buttons-checkbox" data-bind="gender" data-single-select>
                <button type="button" class="btn btn-mini" data-val="1">男</button>
                <button type="button" class="btn btn-mini" data-val="0">女</button>
            </div>
            <lable>性别</lable>
            <%=form.hidden_field :gender, :value=>@q[:gender]%>
            <%#=form.select :gender,options_for_select([["性别",""],["男",'1'],["女","0"]],@q[:gender])%></span>
          -->
          <span><lable>地域选择: </lable>
            <%=form.select :is_beijing,options_for_select(["不限","北京","非北京"],@q[:is_beijing])%>
            <%#=form.select :city,options_for_select([["不限",""]],@q[:city])%>
          </span>

          <%=form.submit "搜索", :class=>"btn btn-info"%>
        </div>
          <!--
        <div class="tags select-unit">

          <span><lable>关注数量: </lable><%=form.select :friends_range,options_for_select([["不限",""],"0-10",'11-50','51-100','101-200','201-500','501-1000','1000+'],@q[:friends_range])%></span>
          <span><lable>粉丝数量: </lable><%=form.select :followers_range,options_for_select([["不限",""],'0-500','501-1000','1001-5000','5001-10000','10001-500000','500001-1000000','1000001-5000000','5000001-10000000','10000000+'],@q[:followers_range])%></span>
          <span><lable>微博数量: </lable><%=form.select :weibo_number,options_for_select([["不限",""],'0-100','101-500','501-1000','1001-5000','5001-10000','10001+'],@q[:weibo_number])%></span>
          <span><lable>注册时间: </lable><%=form.select :reg_time,options_for_select([["不限",""],'0-100','101-200','201-300','301-400','401-500','501-600','601-700','701-800','801-900','901-1000','1000+'],@q[:reg_time])%></span>
          <br/>
          <span>
            <lable>时间范围：</lable>
            <%=form.text_field :start_time, :size=>"10",:placeholder=>"起始",:style=>"width:100px;",:value=>@q[:start_time]%> -
            <%=form.text_field :end_time, :size=>"10",:placeholder=>"结束",:style=>"width:100px;",:value=>@q[:end_time]%>
          </span>
        </div>
        <div class="search-btn"></div>
        -->
        <%end%>
      </div>
    </div>
  <%end%>



    <div class="row-fluid fans-list-box radius-3">
      <div class='title'>
        <strong>
          共有<em><%=@mentions.total_entries%></em>条最新@消息<i> (更新时间 <%=l Date.today%>)</i>
        </strong>

        <%=form_for :search, :url=>pinganbeijing_mentions_path,:method=>:get do |form|
          %>
        <div class="btn-right">
          <div style="float:left">
            发布时间
          </div>
          <div class="date-selector pull-right">
              <%=@date_start%> 至 <%=@date_end%>
          </div>
        </div>


        <div class="btn-right" >
          <div class="input-append input-prepend" style="line-height: 30px;margin: 5px;">
            <span class="add-on"><i class="icon-search icon-black"></i></span>
            <input class="span6" name="q" type="text" value="<%=@serch_key%>">
            <button class="btn" type="submit">搜索</button>
          </div>
        </div>
        <%end%>




      </div>



      <div style="width:100%">
        <div class="left" style="float:left;width:50%;height:100%;">



              <table class='table weibos-list tablesorter'>
                <thead>
                  <tr style="border-top:solid 1px #ccc">
                    <th>内容</th>
                    <th>时间</th>
                    <th>转</th>
                    <th>评</th>
                  </tr>
                </thead>
                <tbody>
          <%@mentions.each{|mention|

            mm = MMention.find(mention.mention_id)
            user = WeiboAccount.find_by_uid(mention.mention_uid)

            url = mention.url
          %>
                <tr data-weibo-id="<%=mention.mention_id%>">
                  <td><%=truncate mm["text"], :length=>50%></td>
                  <td><%=link_to (l Time.parse(mm['created_at'])),url,:target=>"_blank"%></td>
                  <td><%=mm["reposts_count"]%></td>
                  <td><%=mm["comments_count"]%></td>
                </tr>
          <%}%>
                </tbody>
              </table>








          <%
          if false
          @mentions.each{|mention|

            mm = MMention.find(mention.mention_id)
            user = WeiboAccount.find_by_uid(mention.mention_uid)

            url = mention.url
          %>
          <div class='row-fluid'>
            <div class='span12'>
              <table class='table mentions-list'>
                <tr>
                  <td>昵称：<em><%=user.screen_name%></em></td>
                  <td>粉丝数：<em><%=user.followers_count%></em></td>
                  <td>地域：<em><%=user.location.split(" ").first%></em></td>
                  <td>性别：<em><%=user.human_gender%></em></td>
                  <td>认证类型：<em><%=user.human_verified_type.first%></em></td>
                  <td>直发/转发：<em><%=mm['retweeted_status'].blank? ? "直发" : "转发"%></em></td>
                  <td>发布时间：<em><%=l mention.mention_at%></em></td>

                </tr>
                <tr>
                  <td colspan='8'><%=mm.text.gsub("@#{@user_weibo.screen_name}","<b>@#{@user_weibo.screen_name}</b>").html_safe%><br/><%=link_to url,url, :target=>"_blank"%></td>
                </tr>
              </table>
            </div>
          </div>
          <%}
        end
          %>
          <%=will_paginate @mentions%>


          </div>
          <div class="right" style="float:left;width:50%;height:100%;position:relative">
              <div id="interactions" style="background:#fff;padding:20px;border-top: solid 1px #ddd;">
                <ul class="nav nav-tabs interaction-tab">
                  <li class="active" data-type="forwards"><a href="#weibo_forwards_list" data-toggle="tab">转发</a></li>
                  <li class="" data-type="comments"><a href="#weibo_comments_list" data-toggle="tab">评论</a></li>

                </ul>

                  <div id="weibo_forwards_list" class="tab-content active">
                  </div>
                  <div id="weibo_comments_list" class="tab-content">
                  </div>

              </div>

          </div>
        </div>




          
        </div>





  <%content_for :tail do%>

<script type="text/javascript">

$(document).on("click", "[data-toggle='buttons-checkbox'][data-single-select] .btn", function(evt){
    $(this).siblings().removeClass('active');
});


// search btn click event
$(".conditions .btn").click(function(){
  var btn = $(this);
  setTimeout(function(){
    //console.debug(btn.hasClass("active"));
    btn.siblings().removeClass('btn-danger');
    var id = btn.parent(".btn-group").data("bind");
    if(btn.hasClass("active")){
      btn.addClass("btn-danger");

      // set value
      $("#search_"+id).val(btn.data("val"));
    }else{
      btn.removeClass("btn-danger");

      // set value
      $("#search_"+id).val(btn.data(""));
    }
  },1);

})


// search btn default status
$(".conditions input:hidden[id*='search_']").each(function(){
  var h = $(this);
  if (h.val().trim() != ''){
    var id = h.attr("id").replace("search_","");
    h.siblings("div[data-bind='"+id+"']").find("button[data-val='"+h.val()+"']").addClass("active").addClass("btn-danger");
  }
})


  var current_select = null;


  var load_interaction_list = function(type,weibo_id,opts){
    var list = $("#weibo_"+type+"_list");
    $("#weibo_forwards_list").hide();
    $("#weibo_comments_list").hide();
    list.show();
    var params = {mod:"direct",data_type:type,weibo_id:weibo_id,start_date:"<%=@date_start%>",end_date:"<%=@date_end%>"}
    if(opts){
      if(opts.user_type) params.user_type = opts.user_type;
      if(opts.user_group) params.user_group = opts.user_group;
      
    }

    $.get("/pinganbeijing/mentions", params, function(data){
      list.html(data);
    })

  }

  var load_interactions = function(tr){
    
    var interaction_type = $(".interaction-tab li.active").data("type");

    var weibo_id = $(tr).data("weibo-id");
    var padding_top = $(tr).prevAll("tr:last").size() > 0 ? 
                      $(tr).offset().top - $(tr).prevAll("tr:last").offset().top : 0;
    $("#interactions").parent().css("padding-top", padding_top);

    load_interaction_list(interaction_type, weibo_id);

  }


  $(".weibos-list tbody tr").click(function(){
    if(current_select == this) return;
    load_interactions(this);
    $(this).addClass("selected");
    if(current_select) $(current_select).removeClass("selected");
    current_select = this;
  })


  // $("#weibo_comments_list, #weibo_forwards_list").

    $(".weibos-list tbody tr:first").click()


  $(".interaction-tab a[data-toggle='tab']").on('shown',function(e){
    var a = $(e.target);
    var type = a.parent("li").data("type");
    var weibo_id = $("tr[data-weibo-id].selected").data("weibo-id");
    //if($(a.attr("href")).html().trim()==""){
      load_interaction_list(type,weibo_id);
    //}
    console.debug(e.target);
  });




  $(document).on("click","#weibo_forwards_list .pagination a", function(){
    if($(this).attr("href")!="#"){
      $.get(this.href,function(data){
        $("#weibo_forwards_list").html(data);
      });
    }
    return false;
  });

  $(document).on("click","#weibo_comments_list .pagination a", function(){
    if($(this).attr("href")!="#"){
      $.get(this.href,function(data){
        $("#weibo_comments_list").html(data);
      });
    }
    return false;
  });


  $(document).on("change","#forwards_user_type,#comments_user_type,#forwards_user_group,#comments_user_group",function(){
    var action = $(this).data("interaction-type");
    var user_type = $("#"+action+"_user_type").val();
    var user_group = $("#"+action+"_user_group").val();
    load_interaction_list(action,
      $(this).data("weibo-id"),
      {user_type:user_type, user_group:user_group});
    console.info(this);
  })


  $(function(){

    $(".date-selector").dateRangePicker({
      format: 'YYYY-MM-DD',
      separator: ' 至 ',
    }).bind('datepicker-change',function(event,obj)
    {
      console.log(obj);
      // obj will be something like this:
      // {
      //    date1: (Date object of the earlier date),
      //    date2: (Date object of the later date),
      //    value: "2013-06-05 to 2013-06-07"
      // }
    })
    .bind('datepicker-apply',function(event,obj)
    {
      // $(".date-selector").text(obj.date1+" 至 "+obj.date2);
      $(".date-selector").text(obj.value);

      var url = "<%=pinganbeijing_mentions_path(params.select{|k,v| !["start_date","end_date"].include?(k)}).html_safe%>";
      var ds = obj.value.split("至");
      if(url.match('\\?')){
        url = url+"&"
      }else{
        url = url+"?"
      }
      url = url+"start_date="+ds[0].trim()+"&end_date="+ds[1].trim();
      location.href = url;
      console.log(obj);
    })
    .bind('datepicker-close',function()
    {
      console.log('close');
    });;


    $(".date-selector").data('dateRangePicker').setDateRange('<%=@date_start%>','<%=@date_end%>')


    // http://tablesorter.com/docs/
    /*
    $(".tablesorter").tablesorter({sortList:[[2,1]]})
    $(".tablesorter").bind("sortStart",function() { 
    }).bind("sortEnd",function() { 
      var tr = $("tr.selected")
      var padding_top = $(tr).prevAll("tr:last").size() > 0 ? 
                        $(tr).offset().top - $(tr).prevAll("tr:last").offset().top : 0;
      $("#interactions").parent().css("padding-top", padding_top);
    }); 
  */

  })

</script>
  <%end%>


